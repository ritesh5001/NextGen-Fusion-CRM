# Render (and other hosts) set NODE_ENV=production, which makes `npm install`
# omit devDependencies. The TypeScript build needs them (typescript, @types/*),
# so force dev deps to install during the build. `include` overrides the
# NODE_ENV-driven omit.
include=dev
